import java.util.*;

public class Main {
    public static void main(String... args) {
        Scanner b = new Scanner(System.in);
        
        int a = b.nextInt(); 
        for(int i = 1; i <= 10; i++) {
            System.out.println(a + " * " + i + " = " + (a * i));
        }
    }
}
